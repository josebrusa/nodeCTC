const nombre = 'dead pool';
const nombreReal = 'wade winston';

const normal = nombre +' is '+ nombreReal;
const template = `El nombre de superoHero es ${nombre} y su verdadera identidad es ${nombreReal}`

console.log(normal);
console.log(template);


const html = `
<h1>Hola</h1>
<p>Mundo</p>
`;

console.log(html);